# Kokoro TTS v2 - RunPod Serverless Dependencies
# ================================================
# Note: torch and torchaudio are NOT listed here because:
#   - torch 2.5.1+cu124 comes pre-installed in the base Docker image
#   - torchaudio 2.5.1+cu124 is installed separately from the cu124 index
# Listing them here would risk pip installing CPU-only versions.

# Core TTS engine
kokoro>=0.9.4

# Japanese language support
misaki[ja]

# Chinese language support
misaki[zh]

# HuggingFace Hub (for model downloads)
huggingface_hub

# Audio I/O
soundfile>=0.12.1

# RunPod serverless SDK
runpod>=1.7.0

# Numpy
numpy>=1.24.0
